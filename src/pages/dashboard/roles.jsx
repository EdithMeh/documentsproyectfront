import React from "react";
import Layout from "../../components/layout/layout";
const Roles = (props) => {
  const { path } = props;
  return <Layout namePath={path}>Desde Roles!</Layout>;
};

export default Roles;
