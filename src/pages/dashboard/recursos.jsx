import React from "react";
import Layout from "../../components/layout/layout";
const Recursos = (props) => {
  const { path } = props;
  return <Layout namePath={path}>Desde Recursos!</Layout>;
};

export default Recursos;
