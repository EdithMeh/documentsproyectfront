import React from "react";
import Layout from "../../components/layout/layout";
const Proyectos = (props) => {
  const { path } = props;
  return <Layout namePath={path}>Desde Proyectos!</Layout>;
};

export default Proyectos;
